# css
1. scss和sass区别：为了降低学习曲线，scss的语法更贴近原生的css
2. scss语法
	if函数  if( expression, value1, value2 )  类似于三元运算符
	@mixin 创建函数
	@include引入函数
3. 背景图片相对于img标签引入logo的优点：
> 背景图片可以调整大小、位置而不影响布局
> 背景图片可以用配置工具，打为base64编码，放到css代码中
> img标签会带来一次单独的http请求
4. css module
	css scoped	

## 自适应布局
1. viewport的分类：layout-viewport、visual-viewport、ideal-viewport
2. dpr 设备像素比：物理像素/逻辑像素（css像素）
3. width=device-width  设置视口宽度等于屏幕宽度，这里device-width并非分辨率宽度
4. 然后通过设置好的视口的宽度设置rem基准值，再去渲染页面

### 参考博客 <https://blog.csdn.net/u012402190/article/details/70172371>
### rem基准值设置代码：GitHub search hotcss.js 引入该文件即可
其中rem是如何应用的？


# SPA
## 设计意义
1. 前后端分离
2. 减轻服务器的压力：单页面，没有了多页面跳转重新下载的过程，只关注于API接口的调用
3. SPA SEO的问题

## 工作原理
1. history API
2. hash

关键词：记录Ajax刷新，本地路由


# git
1. git checout -b router : 创建一个新的分支router，并切换到该分支	

# 配置工具
## css-loader

# vue 
## nextTick
## v-bind的原理
## 引入插件Vue.use()
## ssr
## export default
## import swiper的css时出错
## width 100% 是如何继承的
## module 和 scoped的区别
## 无缝轮播的实现原理


# webpack 
1. -S就是--save的简写，就行npm默认一个start的字段，你可以不必输入npm run start 而只需输入npm start，这两个效果是一样的。
-D就是--save-dev 这样安装的包的名称及版本号就会存在package.json的devDependencies这个里面，而--save会将包的名称及版本号放在dependencies里面。


# 组件设计的基本思想 
1. 抽象：不断抽象知道不能抽象为止
2. 抽象组件的条件：具备数据驱动、结构不简单、app中复用较多